#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <ctype.h>

int main(int argc, string argv[])
{
        for (int i = 0, n = strlen(argv); i < n; i++)
        {
            printf("Input: %i%c", argc, argv[i]);
            printf("\n");
        }

}
