#include <stdio.h>
#include <cs50.h>


int main(void)
{
    int scores[3];

    score[0] = 72;
    score[1] = 73;
    score[2] = 33;

    printf("Average: %.2f\n", (score1 + score2 + score3) / 3.0);
}