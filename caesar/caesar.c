#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    if (argc < 2 || argc > 2)
    {
        printf("Usage: ./caesar key\n");
    }
    else if ()
    else
    {
        string test = get_string("Plaintext: ");
    }
}