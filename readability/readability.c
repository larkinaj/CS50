#include <cs50.h>
#include <stdio.h>
#include <ctype.h>

int count_letters(string input);

int main(void)
{
    int
    string input = get_string("Text: ");
}

int count_letters(string input)
{
    int length = ;
    return length;
}