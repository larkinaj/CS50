#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <ctype.h>

int main(void)
{
    // string s = get_string("s: ");
    // string t = s;

    // t[0] = toupper(t[0]);

    // printf("s: %s\n", s);
    // printf("t: %s\n", t);

    int i = 5;
    int j = i;

    j = j + 1;

    printf("i: %i\n", i);
    printf("j: %i\n", j);


}