#include <stdio.h>
#include <cs50.h>

int main (void)
{
    int points = get_int("How many points did you lose? ")

    if (points < 2)
    {
        printf("You lost fewer points as me.\n")
    }
    else if 
}